[build]
builder = "nixpacks"

[deploy]
startCommand = "until npx prisma db push; do echo 'DB not ready, retrying in 3s...'; sleep 3; done && next start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
